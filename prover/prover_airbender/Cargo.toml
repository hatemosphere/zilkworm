[package]
name = "z6m_prover_airbender"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "z6m_prover_airbender"
path = "src/main.rs"

[dependencies]
# Core VM (always needed for execution)
riscv_transpiler = { git = "https://github.com/hatemosphere/zksync-airbender", rev = "fb293245", features = ["delegation", "flamegraph"] }

# Proving dependencies
execution_utils = { git = "https://github.com/hatemosphere/zksync-airbender", rev = "fb293245", default-features = false, features = ["prover", "verifier_binaries"] }
prover = { git = "https://github.com/hatemosphere/zksync-airbender", rev = "fb293245", features = ["prover"] }
worker = { git = "https://github.com/hatemosphere/zksync-airbender", rev = "fb293245" }
setups = { git = "https://github.com/hatemosphere/zksync-airbender", rev = "fb293245" }
verifier_common = { git = "https://github.com/hatemosphere/zksync-airbender", rev = "fb293245" }

# GPU prover (optional, requires CUDA)
gpu_prover = { git = "https://github.com/hatemosphere/zksync-airbender", rev = "fb293245", optional = true }

# Shared z6m prover infrastructure (block fetching, RLP encoding)
z6m_common = { path = "../common" }

# Async runtime + networking
tokio = { version = "1", features = ["full"] }
reqwest = { version = "0.12", features = ["json"] }
url = "2.5"

# CLI + serialization
clap = { version = "4.0", features = ["derive", "env"] }
eyre = "0.6"
chrono = "0.4"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
bincode = "1.3"
# bincode 2.x with serde feature for WASM verifier compat (ethproofs/ethereum-prover format)
bincode2 = { package = "bincode", version = "2", features = ["serde"] }
flate2 = "1"
base64 = "0.22"
log = "0.4"

# Logging
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["fmt", "env-filter"] }

[features]
default = ["gpu"]
gpu = ["dep:gpu_prover", "execution_utils/gpu_prover"]
